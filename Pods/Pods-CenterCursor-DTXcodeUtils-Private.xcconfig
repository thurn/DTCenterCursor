#include "Pods-CenterCursor-DTXcodeUtils.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/DTXcodeUtils" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/DTXcodeUtils"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}